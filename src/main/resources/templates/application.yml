spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scheduleApp
    username: root
    password: 85947ads$%
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: always
